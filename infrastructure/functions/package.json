{
  "name": "flightdeck-orchestrator",
  "version": "1.0.0",
  "description": "FlightDeck agent orchestrator — Express server with cron triggers, deployed as a Container App",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js"
  },
  "dependencies": {
    "@azure/identity": "^4.0.0",
    "express": "^5.1.0",
    "node-cron": "^3.0.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
